{
  "name": "@strata/query",
  "version": "0.1.0",
  "type": "module",
  "description": "Read-only queries over a loaded model: search, lineage, impact, the data dictionary and object detail",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@strata/metamodel": "workspace:*",
    "@strata/storage": "workspace:*"
  }
}
