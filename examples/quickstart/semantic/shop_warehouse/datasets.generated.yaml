# Generated by `strata generate semantic` from the shop_warehouse model.
#
# Do not edit. Every change here is overwritten on the next run, and the
# source of truth is the Strata model this came from.
#
# Metrics are deliberately not generated: what revenue means is a business
# decision, not something derivable from a schema. Write them in
# metrics.yaml next to this file, which this command never touches.

version: 0.2.0.dev0
semantic_model:
  - name: shop_warehouse
    description: BigQuery. Real types, real partitioning, real clustering.
    datasets:
      - name: dim_customer
        source: shop_mart.dim_customer
        primary_key:
          - customer_id
        description: One row per customer. Type 1, so a corrected email overwrites the old one.
        fields:
          - name: customer_id
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: customer_id
            datatype: String
            description: The identifier the shop issues to a customer.
            dimension: {}
          - name: customer_name
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: customer_name
            datatype: String
            dimension: {}
          - name: email_address
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: email_address
            datatype: String
            description: Personal data. The classification is inherited from the email_address domain.
            dimension: {}
          - name: created_at
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: created_at
            datatype: DateTimeTz
            dimension: {}
      - name: fct_order
        source: shop_mart.fct_order
        primary_key:
          - order_id
        description: One row per order. Partitioned on the order date because every query filters on it.
        fields:
          - name: order_id
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: order_id
            datatype: String
            dimension: {}
          - name: customer_id
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: customer_id
            datatype: String
            description: Foreign key to dim_customer.
            dimension: {}
          - name: placed_at
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: placed_at
            datatype: DateTimeTz
            dimension: {}
          - name: order_date
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: order_date
            datatype: Date
            description: The partition column. Derived from placed_at in the warehouse timezone.
            dimension: {}
          - name: order_total
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: order_total
            datatype: Decimal
            description: NUMERIC rather than FLOAT64, so money adds up exactly.
          - name: order_status
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: order_status
            datatype: String
            dimension: {}
    relationships:
      - name: fct_order_dim_customer
        from: fct_order
        to: dim_customer
        from_columns:
          - customer_id
        to_columns:
          - customer_id
